# X.page
Live: https://x.page
Scan (after cutover): https://x.page/scan

We make a business citable by AI agents.
Scan a site → fail → paid verified page + llms.txt.
Not a CoS. Not a lead marketplace.

## Repo map
- `apps/web` — public product surface, `/scan`, `/api/scan`, and `/p/[domain]`
- `apps/scan` — serverless scan engine and scoring logic
- `packages/fetch` — thin fetch-engine boundary for optional l2agent integration
