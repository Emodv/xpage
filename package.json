{
  "name": "city-verified",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=@city-verified/web",
    "build": "npm run build --workspace=@city-verified/web",
    "start": "npm run start --workspace=@city-verified/web"
  }
}
